# DISCO 디자인 스펙

> 리빙 도큐먼트입니다. 이 문서와 함께 실제 동작하는 프로토타입 파일(`Main Window.dc.html`, `Settings Window.dc.html`, `Popup Window.dc.html`, `SegmentedControl.dc.html`)을 그대로 Claude Code에 전달하세요.
> **세부 수치·인터랙션은 프로토타입이 우선**이고, 이 문서는 "왜 이렇게 했는지"와 스코프 판단을 기록하는 역할입니다.
> v0.2 — 사용자가 만든 최종 프로토타입 기준으로 전면 갱신

---

## 0. 개요 및 원칙

**기술 스택**: Flutter, `macos_ui`, `flutter_platform_widgets`, `Cupertino` 위젯, SF Pro 폰트 (직접 번들링 필요, Flutter 기본 폰트인 Roboto 대체)

**MVP 단계 디자인 원칙**
1. 커스텀 디자인 시스템(팔레트, 아이콘, 모션) 없음. 시스템 accent color, SF Symbols 대응 아이콘, 기본 트랜지션만 사용. **예외: 팝업창의 vibrancy(반투명 블러) 효과** — 핵심 차별점 화면이라 여기만 시각적으로 투자함.
2. 화면 구조는 하드코딩이 아니라 **데이터 배열을 렌더링하는 방식**으로 설계. 새 채팅방 추가 피커, 설정 리스트 등에 적용. 항목이 늘어나도 배열에 항목만 추가하면 됨.
3. 리스트 기반 화면(설정 등)은 iOS/macOS 공용 컴포넌트인 `CupertinoListSection.insetGrouped`를 플랫폼 분기 없이 그대로 사용.
4. 온보딩, 다크모드 세부 조정은 MVP 이후로 보류.

**전역 컴포넌트 재사용 규칙**
- **리스트 행(List row)**: 아이콘(28×28, 컬러 배경 7px 라운드) + 제목 + 서브텍스트 + chevron. 설정 화면에서 사용.
- **SegmentedControl**: 번역 엔진 선택에 사용, 채팅 툴바와 설정 화면에서 동일 컴포넌트 재사용.
  - 라벨은 **"General" / "Quick"** — 벤더명(Google/Gemini)을 사용자에게 노출하지 않음
  - 내부 값: `general` / `quick`
  - 매핑: **General = Gemini API, Quick = Google Translation API**

---

## 1. 메인 창 (채팅창)

### 1.1 전체 레이아웃 (macOS 기준)
- **타이틀 바**: 52px 높이, `#F7F7F8` 배경, 하단 0.5px 구분선. 좌측 160px 영역에 macOS 트래픽라이트 버튼(빨강/노랑/초록, 표준 앱 종료·최소화·최대화)
- **왼쪽 사이드바**: 160px, 툴바의 토글 버튼으로 show/hide (고정폭, 리사이즈 아님)
- **중앙**: 가변, 채팅 스레드
- **오른쪽 사이드바**: 200px, 툴바의 토글 버튼으로 show/hide. 열려있을 때 타이틀 바 우측에 "단어장" 라벨 표시

### 1.2 왼쪽 사이드바 — 채팅방(주제) 목록 (F-01)
- **채팅방은 언어가 아니라 주제(테마) 단위**입니다. 예: 프로그래밍, 일상, 비즈니스, 여행, 쇼핑, 학교. 실제 번역 언어쌍(모국어→번역 언어)은 세팅창에서 전역으로 한 번만 설정 (2.2 참고)
- 각 행: 텍스트만 (아이콘/아바타 없음)
- 활성 채팅방: `accent` 배경 + 흰색 굵은 텍스트
- 비활성 채팅방: 투명 배경 + 검정(`#1d1d1f`) medium 텍스트
- 최하단 "새 채팅방" 행 클릭 → 프리셋 피커 팝오버 오픈 (색상 점 + 이름, 배열 기반이라 항목 추가만으로 확장 가능)

### 1.3 중앙 — 채팅 스레드
- **툴바**: 채팅방 이름(15px/600) + SegmentedControl(General/Quick, F-04)
- **메시지 버블**: 사용자 → 우측 정렬, `accent` 배경, 흰 텍스트, 말풍선 꼬리(우하단 각짐). 봇(번역 결과) → 좌측 정렬, 옅은 회색(`#F0F0F2`) 배경, 검정 텍스트, 꼬리(좌하단 각짐)
- **번역 대기 중**: 좌측 정렬 버블 안에 점 3개 pulse 애니메이션 (타이핑 인디케이터)
- **입력줄**: 텍스트 필드(placeholder "번역할 텍스트 입력") + 우측 원형 accent 전송 버튼(화살표 아이콘). 입력이 비어있으면 버튼 반투명. Enter 키로도 전송 가능

### 1.4 오른쪽 사이드바 — 단어장 (F-02)
- **각 항목은 플립 카드 칩**입니다: 클릭하면 뒤집혀서 앞면(단어)↔뒷면(뜻, accent 배경)이 전환됨. 별도 플래시카드 화면 없이도 이 사이드바 자체가 가벼운 복습 경험을 제공함
- 방금 자동 저장된 항목은 잠깐 하이라이트(accent 옅은 톤) 후 서서히 원래 스타일로 복귀
- 발음 표기는 MVP 범위에서 제외 (뜻만 표시)
- 자동 저장된 항목만 표시 (수동 추가 없음, F-02 요구사항)
- **빈 상태**: 중앙에 북마크 리본 아이콘(`#c9c9ce`) + "아직 저장된 단어가 없어요"(13px/700) + "채팅에서 번역한 단어가 자동으로 여기 쌓여요"(11px, muted). 버튼 없음

### 1.5 플랫폼 범위
- **1차 출시(MVP)는 macOS 단독**. F-03(전역 단축키 팝업)이 핵심 차별점이자 macOS 네이티브 패턴이라, iOS 버전을 동시에 설계하면 불필요한 디자인 문제를 먼저 풀어야 함
- iOS는 macOS 버전으로 핵심 루프(검색→자동저장)를 검증한 뒤, 별도 스코프로 재설계 예정

### 1.6 외관 커스터마이징 (미정 — 다음 논의)
- 프로토타입은 강조색 8종(블루/퍼플/핑크/레드/오렌지/옐로우/그린/그래파이트 — macOS 시스템 강조색과 동일)과 밀도(comfortable/compact)를 컴포넌트 prop으로 이미 지원함
- 세팅창에 이 옵션을 실제로 노출할지는 아직 미정 (4절 참고)

---

## 2. 세팅 창

### 2.1 목적
전역 단축키(F-03), 번역 엔진 기본값(F-04), 번역 언어쌍 설정

### 2.2 구조
타이틀 바 중앙에 "환경설정" 텍스트. `CupertinoListSection.insetGrouped` 기반, 섹션 구분:

| 섹션 | 항목 | 구조 |
|---|---|---|
| 단축키 | 전역 단축키 | pill 버튼(기본값 `⌘⇧K`). 클릭 시 "키를 누르세요…" 녹화 모드로 전환, 다음 키 조합을 그대로 캡처 |
| 번역 엔진 | 기본 번역 엔진 | SegmentedControl 재사용 (General=Gemini / Quick=Google) |
| 언어 | 모국어 | 선택형 행, 클릭 시 인라인 리스트 확장(영어/스페인어/일본어/한국어), 체크마크로 현재값 표시 |
| 언어 | 번역 언어 | 모국어와 동일한 옵션 셋, 별도 선택 |
| 정보 | 버전 / 문의 | 텍스트만 (예: `1.0.0 (MVP)`, `support@disco.app`) |

### 2.3 참고
- **API 키 입력 UI는 의도적으로 뺐습니다.** 화면에 노출하지 않고 다른 방식(예: 환경변수, macOS Keychain)으로 처리하기로 확정. 구체적인 저장 방식은 아직 미정 (4절 참고)
- 외관(강조색/밀도) 설정 노출 여부도 미정
- 다크모드 대응은 MVP 범위 밖

---

## 3. 팝업 창 (macOS 전용, F-03)

### 3.1 목적
전역 단축키 입력 시 즉시 뜨는 경량 유틸리티 창. Spotlight/Raycast와 동일한 인터랙션 패턴. **MVP 중 유일하게 비주얼 폴리시에 시간을 투자한 화면** — 이 패턴의 정체성 자체가 vibrancy 효과이기 때문

### 3.2 구조
- **창**: 420px 폭, 16px 라운드, 반투명 흰색(`rgba(255,255,255,0.5)`) + `backdrop-filter: blur(34px) saturate(180%)` — 진짜 macOS vibrancy 재질. 실제 앱에서는 `window_manager`로 borderless floating window 구현
- **우측 상단 뱃지**: 현재 엔진 라벨(General/Quick) — 정보 표시용, 토글 아님
- **입력 영역**: 검색 아이콘(돋보기) + 텍스트 필드(18px, placeholder "번역할 텍스트 입력"), 창이 뜨면 자동 포커스
- **자동 트리거 (Enter 불필요)**: 타이핑 멈춘 뒤 약 350ms 후 번역 결과 표시 → 추가 450ms 후 "저장됨" 확인 텍스트(초록 체크, `#34C759`) 노출. 2단계로 나눠서 "번역됨"과 "저장됨"을 순차적으로 인지시킴
- **결과 없음 상태**: "번역 결과가 여기에 표시됩니다" (muted 텍스트)
- **목적지 드롭다운**: 결과 영역 우측, 저장될 채팅방(주제) 이름 + chevron. 클릭 시 채팅방 목록 팝오버, 다른 방 선택하면 이미 입력된 텍스트를 그 방 맥락으로 재번역
- **Esc 키**: 팝업 닫힘/hide. (프로토타입 데모에서는 브라우저 제약상 "다시 열기" 안내로 대체 표시했지만, 실제 macOS 구현에서는 창 자체를 hide하면 됨)

### 3.3 참고
- iOS에는 이 팝업 개념이 존재하지 않음 (OS 제약). iOS 대안은 MVP 이후 논의 (Share Extension 등)

---

## 4. 다음 논의 예정
- [ ] API 키 저장 방식 확정 (환경변수 vs macOS Keychain 등)
- [ ] 외관(강조색/밀도) 설정을 세팅창에 노출할지 여부
- [ ] 다크모드 세부 대응

---

## 5. 컬러 & 타이포그래피

프로토타입에서 실제로 사용한 값 기준입니다.

**컬러 토큰**

| 토큰 | 값 | 용도 |
|---|---|---|
| `accent` (기본) | `#0A84FF` | 강조색 기본값. 사용자가 8종 중 선택 가능(1.6 참고): 퍼플 `#AF52DE`, 핑크 `#FF375F`, 레드 `#FF453A`, 오렌지 `#FF9F0A`, 옐로우 `#FFD60A`, 그린 `#32D74B`, 그래파이트 `#8E8E93` |
| `success` | `#34C759` | 저장 완료 확인 체크 |
| `text-primary` | `#1d1d1f` | 본문, 제목 |
| `text-secondary` | `#6e6e73` | 아이콘, 보조 라벨 |
| `text-muted` | `#86868b` / `#9a9aa0` | 서브텍스트, 캡션 |
| `text-faint` | `#c9c9ce` | 빈 상태 아이콘, placeholder |
| `border` | `rgba(0,0,0,0.08)` | 기본 구분선 |
| `surface-titlebar` | `#F7F7F8` | 타이틀 바 |
| `surface-sidebar-left` | `#F5F5F7` | 왼쪽 사이드바 |
| `surface-sidebar-right` | `#FAFAFA` | 오른쪽 사이드바 |
| `surface-content` | `#FFFFFF` | 채팅 본문, 카드 |
| `surface-window` (설정창) | `#EDEDF0` | 세팅 창 전체 배경 |
| `surface-subtle-fill` | `#F0F0F2` | 봇 말풍선, 인풋 배경, 단축키 pill |
| 트래픽라이트 | `#FF5F57` / `#FEBC2E` / `#28C840` | 창 종료/최소화/최대화 버튼 |

**타이포그래피 (SF Pro, 폰트 파일 직접 번들링 필요)**

| 용도 | 크기 / 굵기 |
|---|---|
| 채팅방 이름 (툴바 타이틀) | 15px / 600 |
| 채팅 메시지 본문 | 14px / 400 |
| 단어장 칩 텍스트 | 12.5px / 600 |
| 설정 행 제목 | 13px / 500 |
| 설정 행 서브텍스트 | 11.5px / 400, `text-muted` |
| 팝업 입력 텍스트 | 18px / 400 |
| 팝업 결과 텍스트 | 15px / 500 |
| 캡션/힌트 | 10.5–12px / 400–600, `text-muted` |

---

## 6. 범위 외 (MVP 이후)

### 6.1 iOS 버전
네비게이션 구조, 팝업 대체 방식 포함해서 별도 스코프로 재설계 예정.

### 6.2 복습 시스템 (F-05, P1 — 전체는 보류, 일부는 이미 메인 화면에 녹아있음)
PRD상 F-05는 P1(P0가 아님)이라 MVP에서는 통째로 뺌. 다만 오른쪽 사이드바의 **플립 카드 칩(1.4)**이 이미 가벼운 복습 경험을 제공하고 있어서, 완전히 빈 상태로 미루는 건 아님. 아래는 "전용 복습 화면"이 필요해질 때 재개할 설계.

**복습 방법 목록 화면** (단어장 사이드바에서 진입, 시트로 오픈)
- `CupertinoListSection.insetGrouped` 패턴, 배열 기반 렌더링
- 항목: 플래시카드(카드 스택 아이콘, "저장된 단어 N개"), PDF로 내보내기(PDF 아이콘, "단어장 전체 저장")
- 확장 원칙: 새 복습 방식 추가 시 `ReviewMethod` 배열에 항목만 추가, UI 구조는 불변

**플래시카드 전용 화면** (사이드바 칩 플립과 별개로, 진행률·키보드 탐색이 있는 몰입형 버전)
- 상단: 닫기(X) + 진행 상태("N / 전체")
- 카드: 클릭 시 3D 플립. 앞면 = 원어 단어, 뒷면 = 뜻만 대형 텍스트
- 하단: 이전/다음 버튼 + 키보드 힌트
- 키보드 단축키: `Space` 뒤집기, `←/→` 이전/다음 — macOS 우선 설계
- 상태 분류("외웠어요/몰라요"), 간격 반복 알고리즘은 미포함

**PDF 내보내기 화면**: 아직 미설계
